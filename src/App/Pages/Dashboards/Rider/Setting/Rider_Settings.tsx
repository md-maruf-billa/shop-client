import { Password_Update_Form } from "@/components/update_password_form";

const Rider_Settings = () => {
      return (
            <div className="flex justify-center items-center min-h-screen">
                  <Password_Update_Form />
            </div>
      );
};

export default Rider_Settings;